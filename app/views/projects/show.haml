-project_face
#frame
  #images
    -@face_image.each do |img|
      =image_tag img.image, :alt => @project.title
  %h1=@project.title

#main 
  -@project.photos.each do |photo|
    =link_to image_tag(photo.image.thumb, :alt => @project.title), url(:photos, :images, :name => photo.image.url[8..100]), :class => 'thumb'
  %p=@project.body 
